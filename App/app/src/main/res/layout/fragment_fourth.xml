<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Заголовок -->
    <TextView
        android:id="@+id/headerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Расшифровка только текстовых файлов"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:marginBottom="16dp" />

    <Button
        android:id="@+id/buttonSelectFile"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Выбрать файл" />

    <TextView
        android:id="@+id/fileNameLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Файл не выбран" />

    <EditText
        android:id="@+id/passwordInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введите пароль"
        android:inputType="textPassword"
        android:minHeight="48dp" />

    <Button
        android:id="@+id/buttonDecrypt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Расшифровать" />

    <Button
        android:id="@+id/buttonSaveInternal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Расшифровать и сохранить" />

    <TextView
        android:id="@+id/outputData"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Результат появится здесь" />

</LinearLayout>
